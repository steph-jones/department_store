<h1> <%= @department.id ? "Edit Department" : "Create Department" %> </h1>
<!--  checks that @department.id exists and if it does edit and if it doesnt new-->
<hr />
<%= form_for @department do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
  <%= f.submit %>
<% end %>